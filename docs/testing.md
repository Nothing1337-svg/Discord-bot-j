# Результаты проверки актуальной версии

Последний локальный аудит: 23 сентября 2026. Живые проверки API ниже выполнены 22 сентября; сегодня они не повторялись. Среда: macOS arm64, Python 3.14.0; версии — `constraints.txt`.

| Проверка | Результат |
|---|---|
| `.venv/bin/pytest -q -W error` | **78 passed**, предупреждения считаются ошибками |
| `.venv/bin/ruff check .` | **All checks passed** |
| `.venv/bin/python -m compileall -q bot tests` | Успешно |
| `.venv/bin/pip check` | No broken requirements found |
| `git diff --check` | Без ошибок |
| `.venv/bin/python -m bot.preview` | 3 PNG 960×540, баннеры прежнего формата |
| `.venv/bin/python -m bot.doctor` | Ожидаемый отказ: Missing DISCORD_TOKEN, Missing GUILD_ID; значения не выдумывались |
| Реальный YouTube Atom Google Developers | **Успех, 15 видео** после настройки CA bundle |
| Реальный PeerTube REST через Framatube, `maschinenraum@tube.tchncs.de` | **Успех, 1 публичное видео** через написанный адаптер |
| Публичный Discord `/api/v10/gateway` | **Успех**, без авторизации и отправки сообщений |
| Реальная отправка в Discord / смена серверного баннера | Не выполнялись: нет реального token, guild ID и настроенных прав |
| Twitch, TikTok, Instagram, Vimeo с реальными аккаунтами | Не выполнялись: нет credentials. Контракты адаптеров проверены локально с имитацией API |
| Docker, Linux/Python 3.11, GitHub Actions | Конфигурации подготовлены; результаты этих запусков не подтверждены |

## Обнаруженное при живых проверках

Изначально Python не находил ни файла, ни каталога доверенных CA; PeerTube выдавал
ClientConnectorCertificateError, системный curl при этом работал. Добавлен certifi и проверяемый TLS context
для платформ и Discord. Проверка сертификата и hostname остаётся включённой (121 доверенный CA в установленном
bundle). После исправления получен успешный ответ YouTube; часть отдельных запросов до/после него завершалась
таймаутами. Это среда с нестабильной доступностью, а не обещание постоянной доступности платформ.

Для федеративных PeerTube-каналов важен полный name@host. Ошибочная попытка найти remote-канал только по
имени на чужом instance вернула 404; запрос с полным идентификатором успешен. Проверены пустой канал и
непустой публичный канал. Адаптер использует предоставленный URL, не угадывает origin instance.

## Что проверено автоматически

- Стабильный baseline, дедупликация после перезапуска, сохранённая очередь и каскадное удаление.
- Независимые повторы получения и доставки; источник недоступен, а сохранённая очередь отправляется;
  Discord недоступен, но новые видео продолжают сохраняться.
- Retry-After, лимит отправок, конкурентные проверки, ограничение размера HTTP и запрет redirects.
- Миграция существующей SQLite без потери истории; старые записи, впервые появившиеся в окне, не
  отправляются для новых подписок при наличии даты публикации.
- YouTube ID/@handle/URL и tabs, разрешение handle через API и отказ без API key.
- Twitch cursor pagination и кеширование app token.
- TikTok pagination, error внутри HTTP 200, видимый лимит страниц, сквозной путь от адаптера до SQLite/доставки.
- Instagram: фильтрация фото/каруселей, только VIDEO; next URL не используется для пересылки bearer token.
- Vimeo: фильтрация private/unlisted/processing видео.
- PeerTube: URL нормализация, запрет внутренних адресов, фильтрация live/private.
- Account aliases, неверный provider, отсутствующий/истёкший token; безопасный вывод doctor.
- Intents, разрешения команд, запрет упоминаний, обработка недостаточных прав канала.
- Голосовой онлайн, AFK/боты/Stage, debounce, cooldown, изменение во время загрузки, недоступный сервер.
- PNG, ru/en, Unicode-графемы и emoji без падения.

Unit-тесты с ответами API не заменяют App Review, OAuth consent и проверку на реальном Discord-сервере.
После настройки нужно добавить подписку на подконтрольного автора, опубликовать **новое** видео и проверить
сообщение. Старые видео специально не отправляются. Для баннера отдельно проверить /banner_preview и
наличие server feature BANNER. Live-старты и пересылка ссылок из сообщений в эту версию не входят.


## Исправления по повторному аудиту 23 сентября

Пять новых regression-тестов сначала упали на прежнем коде и прошли после исправлений:

1. API timestamp с точностью до секунды больше не приводит к потере нового видео в секунду подписки.
2. Пустой RSS GUID заменяется URL; разные записи с пустыми GUID больше не считаются одной записью.
3. Retry-After распознаётся как число секунд и HTTP-date. Повреждённый заголовок не отменяет другой
   корректный Ratelimit-Reset; NaN/Infinity отбрасываются.
4. Ошибка SQLite при чтении списка не уничтожает polling worker; состояние ошибки отображается пользователю,
   восстановление проверяется на следующем успешном проходе. /status отвечает даже при недоступной базе.
5. Остановка отменяет активные slash-команды до закрытия сессии и базы.

Дополнительно реализованы и проверены блокировка повторного процесса (включая отдельный subprocess),
освобождение ресурсов после ошибки startup/sync, повторный close, запрет команд при завершении,
обработка SIGTERM в runner и распространение ошибки login без зависшего ожидания сигнала.

Аудит зависимостей: pip-audit в отдельном временном окружении, команда
`pip-audit --no-deps --disable-pip --progress-spinner off -r constraints.txt --format json`.
Проверены **25 закреплённых пакетов**, пропущенных нет, **известных уязвимостей не найдено**.
Машиночитаемый результат: [dependency-audit.json](dependency-audit.json).
Это проверка доступной базы известных уязвимостей на момент аудита, не гарантия отсутствия неизвестных.

Все исправления проверялись без реального DISCORD_TOKEN и credentials владельцев платформ.
Нельзя подтвердить live-доставку сообщений, права конкретного сервера, App Review или бессрочную работу
стороннего API по одним локальным тестам. Границы поддержки и необходимость продления tokens не изменились.
